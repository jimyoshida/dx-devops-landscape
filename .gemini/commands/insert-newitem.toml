# Description shown in /help
description = "Insert new items into the proper sections"

# The prompt to execute.
# {{args}} will be replaced by the content of a @file or "quoted text".
prompt = """
For each skill/concept/tool/platform item provided, find the most proper website URL.
The website should be its own project/product home or its article on Wikipedia.

Then for each item provided, find its most proper subsection of the skill set.
(e.g., section03.md -> Cloud-Native Computing > Cloud-Native Infrastructure).
You may create a new subsection if there is no proper one for the item.
In the subsection, create and insert the Markdown hyperlink element which label is the concise website title.
Then add the description string of the item just after the hyperlink element.

Input Items:
{{args}}
"""
